const path = require('path');

module.exports = {
  development: {
    username:express_project_dev,
    password:password,
    database:Project_DB,
    host:localhost,
    dialect: 'postgres',
  },
  production: {
    use_env_variable: 'DATABASE_URL',
    dialect: 'postgres',
    seederStorage: 'sequelize',
  }
};
